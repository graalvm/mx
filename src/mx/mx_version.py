# The version must be updated for every PR (checked in CI) and the comment should reflect the PR's issue
version = "7.76.0"  # [repo-root multi-suite improvements] generalize suite selection and skip missing imports
